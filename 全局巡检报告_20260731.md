# 衍界 YanJie AI · 全局巡检报告

> 巡检时间：2026-07-31 14:15 · 模式：日常开发全局检查

## 一、健康指标 ✅

| 维度 | 状态 | 说明 |
|------|------|------|
| 测试套件 | ✅ 104 全绿 | 26 个测试文件，0 失败 0 错误 |
| 架构红线 | ✅ 守住 | `nodes.py`/`scoring.py` 零 LLM 调用，纯函数边界干净 |
| 场景数据 | ✅ schema 统一 | 4 个 JSON（milktea/restaurant/retail/saas）10 键全对齐 |
| 后端模块 | ✅ 全链路完整 | 71 个 .py，core/schemas/engine/agents/api/kb/db/mcp_server/services/middleware/tools 齐备 |
| 数据库 | ✅ 就绪活跃 | yanjie_dev.db + chroma_db 今日均有写入 |
| API 入口 | ✅ 规范 | 中间件栈(RequestId/CORS/Gzip) + 5 路由 + 6 异常处理 |
| 引擎编排 | ✅ 完整 | LLM 路由 + RAG/Tavily/MCP 双模式 + 4 Agent + Judge + 持久化 |
| 配置安全 | ✅ 意图正确 | .gitignore 已保护 .env/.db/chroma_db |

## 二、隐患清单 ⚠️

### 1. 无版本控制（P0 · 最大隐患）
项目根目录**没有 `.git`**，从未 `git init`。
- 71 模块 + 26 测试 + 4 场景全部裸奔，**无任何历史可回溯**。
- AGENTS.md 第 99 行写了 commit 规范，但实际仓库不存在——规范形同虚设。
- 任何一次手滑改动都无法 diff、无法 revert。

### 2. 无项目级虚拟环境（P1 · 可移植性断点）
没有项目内 `.venv`，依赖全部装在全局隔离 venv（`C:\Users\lenovo\.workbuddy\binaries\python\envs\default`）。
- 换机器 / CI / 协作者克隆后**必然断依赖**。
- AGENTS.md 第 62 行明确要求"使用本地虚拟环境，禁止全局安装"，现状违反此规。

### 3. 依赖弃用警告（P2 · 技术债，不阻塞当前）
| 来源 | 警告 | 后果 |
|------|------|------|
| FastAPI TestClient | `httpx` 已弃用 | 建议装 `httpx2` |
| chromadb | EmbeddingFunction 未实现 `build_from_config` | 未来版本将强制 |
| langgraph | `allowed_objects` 默认值将变 | 需显式传参 |

当前不影响运行，但 langgraph 那条升级时可能静默改变行为。

### 4. 遗留垃圾文件（P2）
| 文件 | 性质 | 建议 |
|------|------|------|
| `CLAUDE.md` | 过时副本，已被 7/29 的 AGENTS.md 全面取代 | 删除（避免双源混淆） |
| `lg_probe.py` / `lg_probe2.py` | LangGraph 调试探针，7/30 临时产物 | 删除 |
| `pr.txt` | 临时文本 | 删除 |
| `*.pyc` ×215 | 编译缓存 | 清理（.gitignore 已排除，但不入 git 才有意义） |

## 三、建议行动（按优先级）

- **P0 · 立即**：`git init` → `git add .` → 首次 commit，建立历史基线。否则后续所有改动都是无保护裸奔。
- **P1 · 本周**：建项目级 `.venv` 并 `pip install -e ".[dev]"`，恢复可移植性。
- **P2 · 有空**：清理遗留垃圾（CLAUDE.md / lg_probe* / pr.txt）；跟踪三个弃用警告。
- **P3 · 长期**：langgraph 升级前先确认 `allowed_objects` 显式传参。

---

**结论**：代码层面健康（测试全绿、红线守住、模块完整），隐患全在**工程治理**——无 git、无项目 venv 是两个硬伤，建议优先补齐。
