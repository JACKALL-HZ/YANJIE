# 衍界 YanJie AI · 前端静态托管镜像（Nginx）
# 构建上下文：项目根目录
FROM nginx:1.27-alpine

# 用项目自带的反代配置覆盖默认站点
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf

# 拷贝已构建的前端产物
COPY frontend/dist /usr/share/nginx/html

EXPOSE 80
