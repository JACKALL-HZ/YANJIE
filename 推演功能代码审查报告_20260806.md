# 衍界 YanJie AI · 推演功能代码审查报告（2026-08-06）

> 模式：动态复现（实跑 `general_startup`）+ 只读审查
> 审查范围：`app/engine/*`、`app/agents/inner_graph.py`、`app/api/simulation.py`、`app/api/stream.py`、`.env`、场景 JSON
> 与 7/31 两份报告的关系：前两报告只看了**工程治理**（无 git/venv/测试隔离），从未审查**推演逻辑本身**。本报告的 bug 全部在推演逻辑，且已用代码实跑复现。

---

## 一、结论速览

推演功能有**功能性阻断级 bug**，不只是工程债。用户感知的"跑不出结果 + 数字不讲道理"可归为四条根因：

1. `.env` 的 `PAUSE_EACH_YEAR=1` 把同步无交互入口钉死在第 1 年 → 永远 `paused`、无 `score`。
2. `general_startup` 场景里 4 个 Agent 的决策被整体旁路，量化结果只听**用户消息关键词 + 是否 conservative**。
3. 编排层 `engine.py` 复用 `state.startup_ledger` 时现金状态冻结，季度利润未回写 `finance['remaining_cash']`，破产/回本判定失真。
4. 账本成本模型未校准，任何创业首年必巨亏、必触止损线。

> 注：以上在 stub 与真实 LLM 模式下均成立——`general_startup` 的量化账本是确定性代码，Agent 输出在该场景被丢弃，与 `LLM_USE_STUB` 无关（当前 `.env` 为 `LLM_USE_STUB=0`）。

---

## 二、已复现的硬 bug（附复现证据）

### 🔴 B1　同步/流式推演永远停在第 1 年，`score=None`（最刺眼）

- **现象**：实跑 `iter_events` 事件流为
  `simulation.started → year.started → year.completed(Y1) → simulation.paused`，`timeline` 只有 1 年，`phase=paused / result=None / score=None`。
- **根因**：`.env` 中 `PAUSE_EACH_YEAR=1`，且 `engine.py` 的 `run()`（文档自述"无交互入口"）与 `aiter_events()` 都走逐年暂停分支（`iter_events` line 676-699）。`run()` 只在 `phase=="horizon_review"` 时 `finalize_horizon_review`，但逐年暂停让它每年都 `return`，永远到不了结算。
- **影响**：同步端点 `POST /api/simulations`（`create_simulation`→`engine.run`）返回**半成品**；SSE 流发完 Y1 即 `SIMULATION_PAUSED` 结束。前端（即便做好）也拿不到完整轨迹与评分。

### 🔴 B2　创业场景 4 个 Agent 的决策被整体丢弃

- **根因**：`engine.py:_advance_startup_ledger` 用 `decision_id = self._startup_decision_id(user_msg, strategy)` 选决策，**完全不读** `coordinator.propose()` 返回的 4 个 Agent 动作。外层图 `apply_actions`（`graph.py`）算出的 `world_state` 在 `engine.py` line 523 被 `_sync_startup_state` 用账本整体覆盖。
- **影响**：市场/环境/个人/风险四个智能体对 `general_startup` 是**摆设**；推演数字只听"用户消息关键词"指挥，决策源驱动原则在该核心场景被打破。

### 🔴 B3　策略选择形同虚设：`aggressive` == `steady`

- **复现**（budget=30w/北京/奶茶/3年）：

  | strategy | cust | profit | 结果 |
  |---|---|---|---|
  | steady | 30.9 | -13973 | — |
  | aggressive | 30.9 | -13973 | **与 steady 逐字节相同** |
  | conservative | 22.7 | -15904 | 仅此项不同 |

- **根因**：`_startup_decision_id` 仅在 `strategy=="conservative"` 时给 `defensive`，否则一律 `steady_growth`。`aggressive` 与 `steady` 走同一分支。

### 🔴 B4　用户说"保守"反而触发增长决策

- **复现**：`conversation="我要保守经营，别冒险"`（steady 策略）→ 结果与"什么都不说"**完全相同**（cust=30.9，即 `steady_growth` 扩张）。
- **根因**：关键词表仅有 `{闭店,转让,退出}/{止损,收缩,裁员,缩减}/{明星,代言,加大,扩张,营销,投放,联名}`；"保守/稳妥/谨慎/稳健"不在表中，且非 conservative 策略时直接落 `steady_growth`。用户明确求稳，系统按扩张推演——典型"不符合常理"。

### 🔴 B5　创业账本现金流不累加（冻结）

- **复现**：隔离跑 `YanJieEngine.advance` 现金正常累减（`75000→88685→49409`）；但经引擎包装层 `_advance_startup_ledger` 的一年 4 季度循环，现金**全年冻结在 19098**，而各季利润在 `+875 ~ -45564` 剧烈波动；且 Q2 仍显示"稳健增长"（止损未抑制增长决策）。
- **根因**：`state.startup_ledger` 在 4 季度循环/深拷贝中被复用或别名，`finance['remaining_cash']` 未把季度利润回写。破产判定 `finance['remaining_cash'] <= 0`（yanjie_engine.py line 112）基于冻结值 → `is_game_over` 误判（现金到 0 仍 `is_game_over=False`，见复现输出 `game_over: False, remaining_cash: 19098`）。
- **影响**：创业财务推演数字不可信，破产/回本判定失效。
- **测试盲区**：`tests/test_yanjie_engine.py` 只测孤立 `YanJieEngine`（通过），未覆盖 `engine.py` 编排层，故 CI 抓不到。

### 🟡 B6　成本结构失衡：首季即巨亏

- **复现**：Q1 季度营收 ≈7.8 万 vs 季度成本 ≈14.3 万（成本为营收 1.8 倍），单季亏 4.5~6.6 万；初始化日单 68 杯，Q1 直接跌到 54~83 杯。
- **根因**：`advance` 中 `monthly_cost = revenue*(1-margin) + fixed_cost + extra_cost`，北京奶茶 `fixed_cost≈3.2万/月` + 48% 变动成本，使盈亏平衡点需日单 ~150+ 杯，而初始化仅 68 杯。参数未校准，任何创业首年必亏、必触止损。

### 🟡 B7　`timeline.state_diff` 与真实变化不一致

- **根因**：`engine.py` line 523-531 更新 `timeline[-1].world_state` 与 `agent_actions`，但**没更新 `state_diff`**；`YEAR_COMPLETED` 透出的是外层图（已被丢弃）的 agent 动作增量，而非账本真实变化。前端展示的"本年变化"是错的。

### 🟡 B8　评分 `competition_count` 来源不一致

- 创业场景 `competition_count` 由 Agent 动作增量（如 `market.hold` -2）驱动（20→17），非由量化账本计算；其余场景若 `world_state` 未写入该值则默认 0，`compute_score` 的 `market` 分白送 ~30 分、`risk` 分被压低。评分口径跨场景不统一。

### 🟡 B9（沿用 7/31 报告）　`resume_events` 后续年份硬编码 `"steady"`

- `engine.py` `resume_events` 续跑全部用 `"steady"`，忽略 `strategy_directives`。

---

## 三、根因归类

| 类别 | 对应 bug |
|---|---|
| 配置与入口语义矛盾 | B1（PAUSE_EACH_YEAR 与 run() 无交互语义冲突） |
| 决策源驱动断裂 | B2（Agent 共识/action_effects 被旁路）、B3、B4 |
| 编排层状态同步 bug | B5（startup_ledger 现金冻结/别名）、B7（state_diff 错配） |
| 模型参数未校准 | B6（成本/订单/margin 失衡）、B8（competition_count 口径） |

---

## 四、修复优先级（建议）

- **P0**
  - B1：让同步 `run()` 忽略 `PAUSE_EACH_YEAR`（或在 `run()` 内强制跑完并 `finalize`）；逐年交互只留给 SSE/resume 路径。至少保证 `create_simulation` 能返回 `completed` + `score`。
  - B5：修 `_advance_startup_ledger` 中 `state.startup_ledger` 的状态线程，确保每季 `finance['remaining_cash']` 累加季度利润；补一条"4 季度后现金应单调反映累计利润"的集成测试。
  - B2/B3/B4：统一决策入口——让 4 Agent 共识（或策略）真正驱动 `decision_id`；补齐 `conservative/aggressive/steady` 到账本决策的映射；关键词表加"保守/稳妥/谨慎/稳健"且非 growth 的回退分支。
- **P1**
  - B6：校准 `fixed_cost`/`margin`/`orders`/`quarter_factor`，使盈亏平衡点可达；否则创业永远首年崩。
  - B7：在 `_advance_year` 同步把账本真实 `state_diff` 写回 `timeline[-1]`。
  - B8：统一 `competition_count` 口径（由账本计算或评分前归一）。
- **P2**
  - B9：`resume_events` 续跑读取 `strategy_directives`。

---

## 五、一句话总结

推演"跑不出结果 + 数字不讲道理"的根因不在工程治理，而在：①`PAUSE_EACH_YEAR=1` 把同步入口钉死在 Y1；②创业场景 Agent 决策被旁路、只听关键词；③账本现金冻结、成本模型失衡。先修 **B1 / B2 / B5** 三项，推演才会"跑得完、说得通"。
