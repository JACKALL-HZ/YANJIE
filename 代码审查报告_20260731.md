# 衍界 YanJie AI · 后端全局代码审查报告

> 审查时间：2026-07-31 19:36 · 模式：只读审查（不修改代码）
> 审查范围：全部后端源码（71 个 .py）+ 测试（34 文件 / 232 用例）
> 审查视角：正确性 / 安全 / 可维护性 / 性能 / 测试

---

## 一、总体评价

**架构健康，功能完整度高，测试覆盖充足。** 后端已具备一个可独立运行的企业级推演服务骨架：清晰的四层结构（api → services → engine → agents）、横切层（middleware / core）、完整的数据与知识层。代码红线（纯函数节点、决策源驱动、LLM 只做生成层）基本守住。

**唯一的功能性阻断是「前端为零」**——后端是黑盒，不接前端无法形成可感知的产品。此外 `LLM_USE_STUB=1`，所有 Agent 走预制数据，真模型链路（含慢模型 Judge）从未实调过。

**测试 232 用例，分段运行全绿；但完整串行套件在 ~98 处疑似 chroma_db 锁竞争而停滞（环境问题，非逻辑 bug）。**

---

## 二、开发进度 · 功能地图

### ✅ 已完成的模块

| 域 | 模块 | 关键文件 | 状态 |
|---|---|---|---|
| **引擎编排** | 两层 LangGraph（外层时间线 + 内层 4 Agent + Judge） | `engine/engine.py`, `inner_graph.py`, `graph.py` | ✅ |
| | 评分 / 风险 / 结局判定（纯函数） | `engine/scoring.py`, `ending.py` | ✅ 红线守住 |
| | 干预系统（interrupt + REVIEW/PAUSED + 恢复） | `engine/intervention_graph.py`, `interventions.py`, `engine.resume_events()` | ✅ |
| **Agent** | 市场/环境/个人/风险 4 Agent + Judge | `agents/*.py` | ✅ |
| | 双模式（Stub / 真实 LLM 分层路由） | `core/llm.py` (LLMRouter) | ✅ |
| | 慢模型 Judge 连线（deepseek-reasoner） | `agents/judge.py` `build_judge(slow_llm)` | ✅ 代码就绪，未实调 |
| **RAG / 知识** | 混合检索（bge-m3 + BM25 + RRF + 重排） | `kb/retriever.py` | ✅ |
| | Agent 记忆向量化（Chroma 双 collection + upsert） | `kb/agent_memory_store.py` | ✅ |
| | kb_chunks 双写（Chroma + SQLite 同步） | `kb/ingest.py`, `db/repository.py` | ✅ |
| | Tavily 联网搜索 + MCP 双通道 | `tools/tavily_search.py`, `mcp_server/` | ✅ |
| **API 层** | 11 个端点（见第三节） | `api/*.py` | ✅ |
| | 中间件栈（RequestID / CORS / GZip） | `middleware/*.py` | ✅ |
| | 统一异常处理 + 结构化错误响应 | `main.py` | ✅ |
| **业务编排** | Service 层（Simulation/Scenario/Kb） | `services/*.py` | ✅ |
| | 依赖注入（get_db / get_xxx_service） | `api/dependencies.py` | ✅ |
| **数据层** | 9 张 SQLAlchemy 表 | `db/models.py` | ✅ |
| | 增量持久化（逐事件落库 + session 增量更新） | `engine/engine.py` `_persist_*` | ✅ |
| | 幂等 upsert | `db/repository.py`, `kb/chroma_store.py` | ✅ |
| **配置 / 日志** | 结构化日志（RichHandler + 降级） | `core/logging.py` | ✅ |
| | 配置校验 + 分层配置 | `core/config.py` | ✅ |
| **风险可视化** | 风险传导 DAG 构建（纯函数） | `engine/risk_graph.py` | ✅ 新增 |
| **用户参与** | 行动承诺（commit-actions） | `api/simulation.py`, `test_user_participation.py` | ✅ 新增 |

### 🔴 未完成的模块

| 模块 | 状态 | 影响 |
|---|---|---|
| **前端** | ❌ 零代码（无 `frontend/` 目录） | 后端黑盒，无产品形态 |
| **真模型实调** | ❌ `LLM_USE_STUB=1`，从未跑过真实 LLM | 无法验证推演质量 |
| **慢模型 Judge 实调** | ❌ deepseek-reasoner 未验证 | 评审链路未端到端打通 |
| **端到端压测** | ❌ 未做 SSE 流式 / 长推演压测 | 性能瓶颈未知 |
| **工程治理** | ❌ 无 git、无项目级 venv | 无历史基线、不可移植 |

---

## 三、任务完成清单（按 MVP 阶段）

### MVP-0 后端主体 ✅
- [x] FastAPI 工程脚手架 + 决策源 schema
- [x] 两层 LangGraph 状态机
- [x] 4 个 Stub Agent + 评分/风险/结局判定
- [x] 同步 / SSE 流式 / 对比 端点
- [x] 统一错误兜底
- [x] SQLAlchemy 9 表 + 三层 LangGraph StateGraph + interrupt()
- [x] 场景数据（milktea_startup.json 完整，结局数据驱动）

### MVP-1 LLM 真模型接入 ✅
- [x] `LlmAgent`（prompt 工厂 + JSON 三重提取 + fallback）
- [x] `build_agents` 双模式（stub/LLM）
- [x] RAG 混合检索（bge-m3 + BM25 + RRF + 重排）
- [x] Judge 回合校验（StubJudge + JudgeAgent LLM 评审）
- [x] Tavily Search（httpx + 引擎连线）
- [x] MCP Server（FastMCP + 2 工具 + 双模式 client）
- [x] 数据库持久化（DAO 层 + 增量落库）

### MVP-2 后端补全 ✅
- [x] Judge 慢模型连线（slow_llm 优先级）
- [x] 场景扩充（4 场景：奶茶/零售/SaaS/餐饮）
- [x] API 端点补全（scenarios/sessions）
- [x] Agent 记忆向量化（Chroma 双 collection）
- [x] kb_chunks 双存储同步
- [x] **架构解耦**（schemas 零 engine 依赖 · engine 不握 DB · db 去 FastAPI 概念）
- [x] **横切层补全**（middleware 栈 · services 层 · 结构化日志）
- [x] **交互闭环**（resume 干预恢复 · commit-actions 行动承诺 · state 状态查询 · risk-graph 风险图）

### 测试 ✅（部分环境问题）
- [x] 232 用例 / 34 文件，分段运行全绿
- [ ] **完整串行套件存在停滞**（~98/232 处，疑似 chroma_db 锁竞争）

### 待启动 ❌
- [ ] 前端（Vue3 + Tailwind + Three.js）
- [ ] 慢模型 / 真模型实调验证
- [ ] 工程治理（git init · 项目 venv）
- [ ] 端到端压测

---

## 四、代码质量审查发现

### 🔴 Blockers（必须修）
无功能性阻断。代码逻辑层面未发现会导致数据损坏或服务崩溃的硬 bug。

### 🟡 Suggestions（应该修）

**1. 完整测试套件疑似 chroma_db 锁竞争**
`tests/` 全部 232 用例能收集，分段运行（chroma 相关 25 + 新增 29 + 集成/e2e/stream 9 + 中段 72 + 剩余 20）全绿；但完整串行在 ~98 处停滞。
**Why:** 多个测试文件共用同一 `chroma_db` 持久化目录，连续打开下 SQLite/Chroma 锁可能死锁；叠加 sandbox 的 tmp 清理失败（`SAFE_DELETE_FAIL_CLOSED`），pytest 在收尾阶段卡住。
**Suggestion:** 在 `conftest.py` 用 `tmp_path` 或临时 collection 名为每个测试提供隔离的 Chroma 环境，避免共享持久化目录。这是测试隔离债，不影响生产代码。

**2. SSE 流式端点不持久化（与同步端点行为不一致）**
`api/stream.py` 调 `simulation_service.aiter_events()` 时不传 `db`，推演过程不落库。用户通过 `/stream` 跑的推演在 `simulation_sessions` 表里查不到。
**Why:** 流式场景刻意跳过持久化（避免长连接持库），但导致 `/api/sessions` 历史列表漏掉流式推演，前端若用 stream 则无历史回放。
**Suggestion:** 明确文档化该行为；或在 SSE 结束后补一次最终状态落库（轻量 upsert），保证历史一致性。

**3. resume 后续年份硬编码 "steady" 策略**
`engine.py:634` `self._advance_year(state, session_id, "steady", "", None, ...)` —— 恢复推演后，所有剩余年份固定用 `"steady"` 策略，忽略用户在不同年份可能设置的 `strategy_directives`。
**Why:** resume 仅在暂停点用 `choice`，续跑阶段未传递用户策略意图。
**Suggestion:** 从 `decision_vars` / `strategy_directives` 读取逐年策略，或文档说明「恢复后沿用默认策略」。

**4. checkpointer 默认 MemorySaver（跨进程丢失）**
`engine.py:117` `_build_checkpointer()` 无 `checkpointer_url` 时返回 `MemorySaver()`。单进程 API 可用，但 uvicorn `--workers >1` 或进程重启后，LangGraph 的 interrupt 检查点丢失，resume 无法还原原始暂停上下文。
**Why:** 开发期默认内存 checkpointer 简化，但部署形态未明确。
**Suggestion:** 生产环境在 `.env` 配 `CHECKPOINTER_URL=sqlite:///...` 或 PostgreSQL；并在 AGENTS.md / 部署文档明确 worker 数约束。

### 💭 Nits（建议）

- **遗留垃圾文件**：`CLAUDE.md`（过时副本，已被 AGENTS.md 取代）、`lg_probe.py` / `lg_probe2.py`（调试探针）、`pr.txt`（临时文本）应清理。
- **弃用警告跟踪**：`httpx`（TestClient）、`chromadb`（EmbeddingFunction.build_from_config）、`langgraph`（allowed_objects 默认值）三条弃用警告，升级时可能静默改变行为。
- **`simulation.py:86-92` commit-actions 直接 mutate ORM JSON 字段**：逻辑正确（随后 `srepo.update`），但建议显式 copy 避免意外副作用。
- **`ResumeRequest.choice` 为自由字符串**：未校验 `choice` 是否在场景允许的 `intervention_choices` 集合内，非法 choice 会进入引擎。

---

## 五、架构评价（对比企业级标准）

| 层 | 包 | 评价 |
|---|---|---|
| 接口层 | `app.api` | ✅ 端点清晰，统一错误响应，依赖注入规范 |
| 中间件层 | `app.middleware` | ✅ 横切关注点独立，注册顺序正确（RequestID→CORS→GZip） |
| 业务编排层 | `app.services` | ✅ API→Service→Engine 职责清晰，不调 LLM、不做判定 |
| 领域/编排层 | `app.engine` | ✅ 不直接握 DB（db 由调用方注入）；纯函数节点守住红线 |
| 领域模型层 | `app.agents`, `schemas` | ✅ `schemas` 零 `engine` 反向依赖（已解耦） |
| 基础设施层 | `app.db`, `kb`, `tools`, `mcp_server` | ✅ 单向依赖，Chroma 双 collection 隔离干净 |
| 横切层 | `app.core` | ✅ config/logging/llm/errors 零反向依赖 |

**依赖方向已健康**：`api → services → engine → agents → schemas.domain_models`，无循环依赖，无下层反向 import 上层。

---

## 六、测试状态

- **收集**：232 用例 / 34 文件，全部可收集
- **分段运行**：5 批（chroma 25 + 新增 29 + e2e/stream 9 + 中段 72 + 剩余 20）全绿，exit=0
- **完整串行**：~98/232 处停滞（环境问题，非逻辑 bug）
- **覆盖亮点**：干预恢复、行动承诺、风险图、checkpoint、端到端 replayable 均有专门测试
- **缺口**：无前端集成测试（无前端）、无真实 LLM 集成测试（stub 模式）、无并发/压测

---

## 七、下一步建议（优先级）

1. **P0 · 工程治理**：`git init` → 首次 commit，建立历史基线；建项目级 `.venv`。
2. **P0 · 修测试隔离**：`conftest.py` 为 Chroma 提供临时/隔离环境，消除完整串行锁竞争。
3. **P1 · 实调验证**：切 `LLM_USE_STUB=0` 跑一次完整推演，验证真模型 + 慢模型 Judge + Tavily 链路。
4. **P1 · 前端启动**：Vue3 + Tailwind + Three.js，先做场景选择 + 推演主界面 + 时间线回放。
5. **P2 · 清理债务**：删遗留文件；配置 `CHECKPOINTER_URL`；统一 SSE 持久化策略。
6. **P3 · 压测**：SSE 流式 + 长推演（5 年）端到端性能基线。

---

**结论**：后端工程化水平已达 MVP 可交付门槛，架构规范、测试充分、红线守住。卡点不在代码质量，而在「未接前端 + 未实调真模型 + 工程治理缺失」三件事。优先补齐 git/venv 与测试隔离，再切真模型跑一次，即可进入前端联调阶段。
