# 衍界 YanJie AI · 项目技术架构详解

> **用途**：答辩 / 面试技术介绍参考文档
> **定位**：AI 决策推演工作台 — 让每一个选择都有回响
> **日期**：2026-08-10

---

## 目录

1. [项目总览与进度评估](#1-项目总览与进度评估)
2. [整体架构](#2-整体架构)
3. [推演主链路](#3-推演主链路)
4. [LangGraph 三层编排](#4-langgraph-三层编排)
5. [Agent 工作流与提示词工程](#5-agent-工作流与提示词工程)
6. [RAG 混合检索管道](#6-rag-混合检索管道)
7. [MCP 工具模式](#7-mcp-工具模式)
8. [记忆持久化与上下文管理](#8-记忆持久化与上下文管理)
9. [评分 / 结局 / 风险 / 干预](#9-评分--结局--风险--干预)
10. [LLM 分层路由](#10-llm-分层路由)
11. [安全与中间件](#11-安全与中间件)
12. [数据库设计](#12-数据库设计)
13. [答辩 Q&A 预演](#13-答辩-q-a-预演)

---

## 1. 项目总览与进度评估

### 1.1 项目定位

衍界 YanJie AI 是一个 **AI 决策推演工作台**。用户选择一个人生决策场景（创业、考研、留学、买房等），系统通过多 Agent 辩论 + LangGraph 状态机逐年推演，给出评分、风险、结局和行动建议。

核心设计红线：**决策源驱动，LLM 只做生成层**。`scenarios/*.json` 决定事实、约束和判定规则；LLM 不决定关键结局——结局判定是纯阈值比较。

### 1.2 技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue3 + TypeScript + Tailwind + Three.js + Pinia + ECharts |
| 后端 | Python 3.12 + FastAPI + LangGraph + LangChain |
| 数据库 | SQLite（开发）/ PostgreSQL（生产） |
| 向量库 | ChromaDB（开发）/ pgvector（生产） |
| LLM | DeepSeek（快模型 chat / 慢模型 reasoner） |
| Embedding | bge-m3 via SiliconFlow |
| Reranker | bge-reranker-v2-m3 via SiliconFlow |
| 工具协议 | MCP (Model Context Protocol) |

### 1.3 代码规模

| 维度 | 数量 |
|---|---|
| 后端 Python 源文件 | 101 个（~13,564 行） |
| 测试文件 | 61 个（~7,621 行） |
| 场景 JSON | 11 个（创业 5 + 考研 + 留学 + 职业晋升 + 求职 + 买房 + 投资） |
| 前端组件 | 54 个（10 页面 + sim/chat/charts/ui/motion/three/layout/auth/ask 组件） |

### 1.4 进度评估：接近末期

**后端（MVP-0 + MVP-1 全部完成）**：
- 推演引擎（SimulationEngine 1401 行 + YanJieEngine 189 行量化创业模拟）
- 两层 LangGraph + 干预图（外层 4 节点线性 + 内层 6 节点带条件边循环 + 干预 interrupt）
- 4 Agent 双模式（Stub 规则 / LLM DeepSeek）+ Judge 修订循环
- RAG 混合检索（向量 + BM25 + RRF + Reranker）
- MCP Server（4 工具 + inline/stdio 双模式）
- SQLAlchemy 9 表 + 7 DAO 仓库 + 增量落库
- FastAPI 11 路由 + SSE 流式 + JWT 认证 + 7 层中间件
- 11 场景完整数据驱动

**前端（主体完成）**：
- 10 页面 + dist 产物
- Three.js 分支时间线 + ECharts 评分雷达 + SSE 实时推演流

**剩余工作**：前端联调收尾、知识库扩充、生产部署适配。

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端 (Vue3 + TS)                            │
│  LibraryView → SimView → CompareView / ProfileView / DiaryView     │
│         ↕ HTTP / SSE                                                 │
├─────────────────────────────────────────────────────────────────────┤
│                      FastAPI (main.py)                              │
│  7 层中间件: RequestId → ApiKey → RateLimit → BodyLimit             │
│              → CORS → Gzip → SecurityHeaders                        │
│  11 路由: health/ask/assistant/diary/profiles/report               │
│           /simulation/stream/scenarios/sessions/auth                │
├─────────────────────────────────────────────────────────────────────┤
│              SimulationEngine (engine.py) · 总指挥                      │
│  逐年的「总指挥」：手动按序调用三张独立编译的图（非箭头直连）                                      │
│                                                                      │
│  ① 内层图 inner_graph.py  （引擎先调，产出 actions）                             │
│     observe→propose→judge→[revise↺]→validate→emit                    │
│     └ 4 Agent(Market/Env/Personal/Risk)+Judge 并行提案+修订循环              │
│                                                                      │
│  ② 外层图 graph.py  （actions 传入，负责落地+判结局+归档）                            │
│     prepare→apply_actions→check_ending→append_year→END               │
│                                                                      │
│  ③ 干预图 intervention_graph.py  （每年后查，可暂停）                             │
│     check_and_apply→interrupt()暂停→等 /resume→恢复                       │
│                                                                      │
│  三图共享一个 checkpointer（存档），thread_id 不同：                               │
│  outer-{year} / iv-{year}；引擎逐年循环 + SSE 推送前端                          │
├─────────────────────────────────────────────────────────────────────┤
│  MCP Server (yanjie-mcp)          │  RAG 知识库 (kb/)               │
│  4 工具: search_knowledge         │  HybridRetriever:               │
│         search_web               │  向量(bge-m3) + BM25 + RRF       │
│         assess_execution_capacity │  + Reranker(bge-reranker-v2-m3) │
│         run_risk_stress_test      │  场景路由: classify_scene        │
├───────────────────────────────────┼─────────────────────────────────┤
│  数据库 (db/)                     │  核心配置 (core/)                │
│  9 表 SQLAlchemy                  │  LLMRouter(FAST/SLOW/STUB)      │
│  7 DAO 仓库                       │  sanitize 防注入                │
│  幂等迁移                         │  Settings(.env 驱动)            │
└───────────────────────────────────┴─────────────────────────────────┘
```

### 架构边界（红线）

| 红线 | 说明 |
|---|---|
| 决策源驱动 | `scenarios/*.json` 决定事实和判定规则，LLM 不决定关键结局 |
| 纯函数节点 | `nodes.py` 不直接调 LLM；LLM 调用放 `agents/`；评分放 `scoring.py` |
| Prompt 分离 | System / User Prompt 严格分离，外部内容用 `[UNTRUSTED]` 标签包裹 |
| 协议层约束 | 模型输出优先用 `response_format` / Function Calling Schema，不只靠 Prompt |
| 最小权限工具 | 每个工具超时 + 重试 + 熔断；高危操作切 `REVIEW` / `PAUSED` |

---

## 3. 推演主链路

### 3.1 端到端流程

```
用户选择场景 → POST /api/simulations
  │
  ├─ 1. 加载 DecisionSource (scenarios/*.json)
  ├─ 2. 冻结用户画像到 session.user_profile (JSON 快照)
  ├─ 3. 初始化 SimulationState (world_state + decision_vars + timeline)
  ├─ 4. 构建 SimulationEngine:
  │     ├─ AgentCoordinator (4 Agent + Judge + RoleToolRouter)
  │     ├─ 外层 Graph (build_outer_graph + checkpointer)
  │     ├─ 干预 Graph (build_intervention_graph)
  │     └─ SimulationRepo / EventRepo / MessageRepo (持久化)
  │
  └─ 5.逐年推演 (iter_events / aiter_events) — 由 SimulationEngine 逐年循环驱动，
        │   三张图不是箭头直连，而是引擎按序手动调用：
        │
        FOR year = 1 to max_years:
        │
        ├─ 5.1 引擎先调【内层图】coordinator.propose() → 4 Agent 辩论 + Judge 修订，产出 actions
        │     ├─ observe() — 为 4 个 Agent 构造决策摘要 + 工具证据 (RoleToolRouter 白名单)
        │     │     └─ MCP 优先 / Retriever 降级 / 本地 fallback
        │     ├─ propose_actions() — ThreadPoolExecutor 并行请求 4 个 Agent
        │     │     └─ LlmAgent: System+User Prompt → LLM → JSON 三重提取 → AgentAction
        │     ├─ judge_round() — Judge 评审冲突
        │     │     ├─ judge_ok=true → validate → emit
        │     │     └─ judge_ok=false + revisions < 2 → revise(注入反馈+收窄白名单) → 回 propose
        │     ├─ validate() — 校验 action_id 合法性 + 中文可读性
        │     └─ emit() — 深拷贝输出 AgentAction 列表
        │
        ├─ 5.2 引擎再调【外层图】_graph.invoke(actions) → 把方案落地 + 判结局 + 归档
        │     ├─ prepare() — 深拷贝 state, 设 phase=simulating
        │     ├─ apply_actions() — 把内层图产出的 actions 应用到 world_state (reducers.py)
        │     ├─ check_ending() — 纯阈值比较判定结局
        │     │     └─ bankrupt / goal_reached / steady_state / timeout
        │     └─ append_year() — 追加到 timeline
        │
        ├─ 5.3 引擎再调【干预图】_iv_graph.invoke(state) → 查干预，可能暂停
        │     ├─ 有干预且未选 → interrupt() 暂停 → yield INTERVENTION_PENDING + PAUSED
        │     └─ 用户 /resume 提交 choice → 恢复 invoke → 应用效果
        │
        ├─ 5.4 _persist_agent_events() — 当年事件落库
        ├─ 5.5 _persist_session_state() — session 状态更新
        │
        └─ 5.6 yield SSE 事件 (agent_proposals / judge_result / world_state / year_summary)

        END FOR
        
        ├─ 6. compute_score() — 四维评分 (场景化标签 + 权重 + 结局加成)
        ├─ 7. extract_risks() — 场景化风险提取 (7 套各 5 条)
        ├─ 8. build_action_plan() — 行动计划模板 (36 个场景化模板)
        └─ 9. judge_ending() — 最终结局判定 + 持久化
```

### 3.2 逐年交互模式

推演不是一次性跑完。每年推演后暂停，等待用户决策输入：

```
year N 完成 → yield SSE 事件 → 暂停 (PAUSED)
  ↓
用户输入:
  ├─ 决策选择 → /resume → 恢复推演
  ├─ 提问 → 意图分类(question) → 问答服务 → 不影响推演
  ├─ 闲聊 → 意图分类(casual) → 轻量回复
  └─ 业务决策 → 意图分类(business_decision) → Personal Agent 处理
  ↓
year N+1 → ...
```

**年限确认机制**：达到预设年限后进入 `horizon_review` 阶段，用户可选：
- `finalize_simulation` — 结束结算
- `extend_1_year` — 续推 1 年
- `extend_3_years` — 续推 3 年

**决策预览**：每年可生成 3 分支方案供用户选择：
- `user_proposal` — 用户提案
- `expert_recommendation` — 专家建议
- `low_cost_alternative` — 低成本替代

---

## 4. LangGraph 三层编排

这是项目的核心架构亮点。用三个 LangGraph StateGraph 实现「时间线推进 / Agent 协调 / 人工干预」三层分离。

### 4.1 外层 Graph — 时间线推进

**文件**：`app/engine/graph.py` (79 行)

```
prepare → apply_actions → check_ending → append_year → END
```

| 节点 | 纯函数 | 职责 |
|---|---|---|
| prepare | `prepare_state()` | 深拷贝 state + 设 phase=simulating + 清暂停态 |
| apply_actions | `apply_supplied_actions()` | 委托 `reducers.apply_actions` 应用效果到 world_state |
| check_ending | `check_ending()` | 委托 `ending.judge_ending` 纯阈值比较 |
| append_year | `append_year()` | 委托 `reducers.append_timeline` 追加时间线 |

**设计要点**：
- 节点只编排不重写业务逻辑——全部委托 `nodes.py` 纯函数
- 可选 checkpointer（MemorySaver / SqliteSaver / PostgresSaver）支持断点续推
- `OuterState(TypedDict)` 承载 `state`、`actions`、`transition`、`ending`

### 4.2 内层 Graph — Agent 协调

**文件**：`app/agents/inner_graph.py` (739 行)

```
observe → propose → judge ──→ validate → emit → END
                   │ judge_ng
                   └→ revise → (回 propose)
```

**条件边路由** `_route_after_judge`：
```
if judge_result.judge_ok:
    → validate
elif revision_count < max_revisions (默认 2):
    → revise (注入 judge 反馈 + 收窄动作白名单) → propose
else:
    → validate (达到修订上限，强制通过)
```

**AgentCoordinator 核心方法**：

| 方法 | 职责 |
|---|---|
| `observe()` | 为 4 个角色构造同一决策摘要 + 各自工具证据（RoleToolRouter 按角色白名单分配） |
| `propose_actions()` | ThreadPoolExecutor 并行请求 4 个 Agent |
| `judge_round()` | Judge 评审 + 注册冲突约束 |
| `validate()` | 校验 action_id 合法性 + 中文可读性 + 回退机制 |
| `emit()` | 深拷贝输出 |
| `propose()` | 统一入口，invoke 内层 graph |
| `build_debate()` | Judge 冲突转 DebateRecord（用户可见辩论记录） |

**InnerState(TypedDict)** 关键字段：
- `contexts` — 4 个 AgentContext
- `actions` — 4 个 AgentAction
- `judge_result` — JudgeResult
- `yearly_strategy` — Personal Agent 策略（其他 Agent 知晓但不强制遵循）
- `revision_count` / `max_revisions` — 修订循环控制

### 4.3 干预 Graph — 人工干预

**文件**：`app/engine/intervention_graph.py` (82 行)

```
check_and_apply → [interrupt() 暂停] → 等待外部 choice → 恢复 → 应用效果
```

**机制**：
1. 检测待决干预 `find_pending_intervention()`
2. 若 `choice is None` → `interrupt(pending.model_dump())` 暂停
3. 暂停期间 yield `INTERVENTION_PENDING` + phase=`PAUSED`
4. 用户通过 `/resume` 端点提交 choice
5. 恢复 invoke，`apply_intervention()` 应用效果，记录 `InterventionRecord`
6. 更新 timeline 最后一节点的 world_state 和 interventions

**关键设计**：`interrupt()` 是 LangGraph 的原生能力——图暂停在某个节点，状态持久化到 checkpointer，外部恢复后从断点继续。

### 4.4 三层编排的价值

| 问题 | 解决方案 |
|---|---|
| 时间线推进和 Agent 协调耦合 | 外层/内层分离，各自独立编译 |
| Agent 决策冲突无仲裁 | Judge 修订循环（条件边 + 最多 2 次重试） |
| 关键节点需人工介入 | 干预图 interrupt() 暂停/恢复 |
| 长时间推演可能中断 | Checkpointer 断点续推 |
| 纯函数和副作用混在一起 | 节点编排和业务逻辑分离 |

---

## 5. Agent 工作流与提示词工程

### 5.1 四角色 Agent

| 角色 | agent_id | 立场 | 工具白名单 |
|---|---|---|---|
| 市场 Agent | `market` | 追求市场份额和差异化 | search_knowledge |
| 环境 Agent | `environment` | 关注外部环境和政策 | search_knowledge |
| 个人 Agent | `personal` | 用户画像代言，追求稳健 | search_knowledge + assess_execution_capacity |
| 风险 Agent | `risk` | 保守，关注风险对冲 | search_knowledge + run_risk_stress_test |

**Personal Agent 特殊性**：其 `yearly_strategy` 是本年策略指令，其他 Agent 知晓但不强制遵循——只有 Personal Agent 受其约束。

### 5.2 AgentProtocol 协议

**文件**：`app/agents/contracts.py`

```python
class AgentProtocol(Protocol):
    agent_id: str
    def propose(self, context: AgentContext) -> AgentAction: ...
```

**AgentContext**（frozen dataclass，20+ 字段）：
- `agent_id`, `year`, `world_state`, `decision_vars`
- `allowed_action_ids` — 白名单（Judge 反馈后会被收窄）
- `agent_stance` — 角色立场
- `rag_context`, `search_context` — 检索结果
- `yearly_strategy` — Personal Agent 策略
- `user_profile_summary`, `user_message` — 用户画像和消息
- `judge_feedback`, `constraint_note` — Judge 修订反馈
- `evidence` — 工具调用证据
- `scenario_id`, `scenario_title`, `scenario_brief` — 场景上下文

### 5.3 LlmAgent 提示词工程

**文件**：`app/agents/llm_agent.py` (587 行)

#### System Prompt 结构

```
[角色身份] 你是{scenario_title}场景中的{role}角色。
[立场]     你的核心关切：{agent_stance}
[目标]     在当前世界状态下，从允许的动作中选择一个。
[输出格式] 严格 JSON: {"action_id": "xxx", "reasoning": "xxx", "confidence": 0.0-1.0}
[边界约束]  不要编造动作、不要超出白名单、reasoning 用中文。
```

#### User Prompt 结构

```
[年份]     当前是第 {year} 年
[决策变量]  {decision_vars}
[专属简报]  {scenario_brief}
[世界状态]  {world_state 定性化描述}    ← 不是原始数值！
[允许动作]  {allowed_action_ids + action_descriptions}
[策略指令]  {yearly_strategy}           ← Personal Agent 强制遵循
[RAG上下文] [UNTRUSTED_RAG] {清洗后的检索内容} [/UNTRUSTED_RAG]
[搜索上下文] [UNTRUSTED_SEARCH] {清洗后的搜索内容} [/UNTRUSTED_SEARCH]
[Judge反馈] {judge_feedback}            ← 修订循环时注入
```

#### 世界状态定性化 `_format_world_state()`

**核心思路**：不让 LLM 看到精确数值，防止复述数值而非推理。

```python
# 原始: cash_flow = 35000
# 定性化: "资金缓冲较为紧张" (cash_flow < 50000)
# 原始: competition_count = 8
# 定性化: "竞争压力较大" (competition_count > 5)
```

场景化标签映射：
- 创业场景：cash_flow, monthly_profit, customer_flow, competition_count, payback_ratio
- 留学场景：language_score, application_progress, budget_status
- 买房场景：budget, market_price_trend, loan_pressure
- 投资场景：portfolio_value, market_volatility, risk_tolerance

#### 安全处理

1. **用户输入清洗** `sanitize_user_input()`：截断 + 去控制字符 + 7 种注入模式检测
2. **RAG 内容清洗** `sanitize_rag_content()`：屏蔽 traceback + 文件路径
3. **不可信标签包裹**：所有外部内容用 `[UNTRUSTED_RAG]` / `[UNTRUSTED_SEARCH]` 标签包裹

#### JSON 解析三重容错 `_json_from_text()`

```python
def _json_from_text(text: str) -> dict:
    # 1. 直接 json.loads — 最理想情况
    # 2. 正则提取 markdown 代码块 ```json ... ```
    # 3. 栈平衡括号匹配 — 最后兜底
```

解析失败时回退到 `allowed[0]` + 低置信度。

#### 非法动作降级

```python
if action_id not in allowed_action_ids:
    # 降级到 allowed[0]，confidence 降为 0.3
```

### 5.4 StubAgent — 确定性模式

**文件**：`app/agents/base.py`

```python
# 基于 (year + len(agent_id)) % len(allowed) 确定性选择
# Judge 反馈存在时偏移一位
# position 映射: market=support, risk=oppose, 其他=conditional
```

零外部依赖，测试环境友好。

### 5.5 Judge 修订循环

**文件**：`app/agents/judge.py` (281 行)

**StubJudge（规则检测）**：
- 冲突对检测：3 组冲突动作对（如 market.differentiate vs risk.contain）
- 总现金消耗阈值：> 25000 触发冲突；安全线 60000
- 全体激进检测：4 个 Agent 同时扩张
- `severity = min(1.0, conflicts * 0.25)`；`judge_ok = severity < 0.5`

**JudgeAgent（LLM 评审）**：
- System prompt 描述冲突模式
- 解析 JSON → `JudgeResult`
- 中文可读性降级

**修订循环**：
```
Judge 检测冲突 → judge_ok=false
  → revise 节点注入 judge_feedback + constraint_note
  → 收窄非 risk Agent 到防御性动作 (hold/monitor/defer/contain/insure/pause)
  → 回退 propose
  → 最多 2 次修订
```

**冲突约束** `conflict_resolution.py`：
- `severity >= 0.5` 或 `risk agent oppose` → 生成 `AgentConstraint`
- 每个 agent 有独立的中文约束文案

---

## 6. RAG 混合检索管道

### 6.1 整体管道

```
用户查询
    │
    ├─ 1. 场景路由 classify_scene()
    │     ├─ 一级: 关键词精确匹配 (5 领域)
    │     └─ 二级: LLM 兜底分类 (6 类)
    │     → collection = decision_kb_{domain}
    │
    ├─ 2. 向量召回 (bge-m3, top-20)
    │     └─ ChromaStore.search(query, collection, top_k=20)
    │
    ├─ 3. BM25 关键词召回 (top-20)
    │     └─ rank_bm25 Okapi BM25
    │     └─ 中文分词: 单字 + 英文词 + 数字
    │
    ├─ 4. RRF 融合 (Reciprocal Rank Fusion, k=60)
    │     └─ score = Σ 1/(k + rank_i)
    │     └─ 去重合并
    │
    ├─ 5. Reranker 重排 (bge-reranker-v2-m3)
    │     └─ SiliconFlow API 调用
    │     └─ 返回 top-k
    │
    └─ 最终结果 (带 source/score 元数据)
```

### 6.2 Ingest 摄入管道

**文件**：`app/kb/ingest.py`

```
Markdown 文档
    │
    ├─ load_and_split()
    │   ├─ RecursiveCharacterTextSplitter(chunk_size=600, overlap=80)
    │   ├─ Markdown 优先分隔符: ## → ### → #### → 段落 → 句号 → 分号 → 逗号
    │   ├─ 元数据解析: YAML frontmatter + Markdown 注释 (> 知识类型: xxx)
    │   └─ DocChunk: chunk_id = {source}-{idx:03d}
    │
    ├─ SiliconFlowEmbedder.embed() — 批量 bge-m3 向量化 (1024 维)
    │
    └─ 双写:
        ├─ ChromaStore.upsert() — 向量库 (cosine, PersistentClient)
        └─ KbChunkRepo.save_batch() — SQL 表 (幂等)
```

**幂等设计**：`store.upsert()` 覆盖同 ID chunk；SQL `save_batch` 幂等写入。

### 6.3 场景路由

**文件**：`app/kb/classify_scene.py`

**双级策略**：
1. **一级 — 关键词精确匹配**：
   - education: 考研/留学/考研/研究生
   - entrepreneurship: 创业/开店/公司/餐厅
   - career: 职业/晋升/跳槽/求职
   - housing: 买房/房价/房贷
   - investment: 投资/股票/基金/理财

2. **二级 — LLM 兜底分类**（6 类：创业/教育/职业/买房/投资/其他）

**创业意图正则**：
```python
r"(?:开|经营|创办|做)\s*(?:一家|一个)?\s*[^，。；,;]{0,12}(?:店|馆|餐厅|公司|工作室|摊)"
```

**集合路由**：`get_collection_for_domain(domain)` → `decision_kb_{domain}`

### 6.4 VectorStore 抽象层

**文件**：`app/kb/vector_store.py`

```python
@runtime_checkable
class VectorStore(Protocol):
    def add(self, ids, embeddings, documents, metadatas): ...
    def upsert(self, ids, embeddings, documents, metadatas): ...
    def search(self, query_embedding, top_k, collection): ...
    def get_all(self, collection): ...
    def count(self, collection): ...
    def delete_collection(self, collection): ...
    def delete(self, ids, collection): ...
```

**ChromaStore 实现**：PersistentClient + cosine + PassthroughEmbedding（外部已生成向量，Chroma 不重复计算）

**生产切换**：新增 `PgVectorStore` 实现同一 Protocol，改 config 即可切换。

### 6.5 Agent 长期记忆

**文件**：`app/kb/agent_memory_store.py`

```python
class AgentMemoryStore:
    # 基于 Chroma，按 user_id 隔离
    def save(self, user_id, agent_id, domain, key, value, weight=1.0): ...
    def search(self, user_id, query, top_k=5): ...  # 过滤 user_id
    def delete_by_domain(self, user_id, domain): ...
    # 记忆 ID 格式: {user_id}:{agent_id}:{domain}:{key}
```

### 6.6 容错降级链

| 故障点 | 降级策略 |
|---|---|
| Embedding 服务不可用 | 纯 BM25 检索 |
| Reranker API 故障 | 返回 RRF 融合结果（不重排） |
| Chroma 故障 | 场景规则兜底（评分/风险仍可用） |
| MCP 工具调用失败 | 本地 fallback（确定性评估） |

---

## 7. MCP 工具模式

### 7.1 MCP Server

**文件**：`app/mcp_server/server.py`

```python
mcp = FastMCP(name="yanjie-mcp")

@mcp.tool()
def search_knowledge(query: str, scenario_id: str) -> str: ...

@mcp.tool()
def search_web(query: str) -> str: ...

@mcp.tool()
def assess_execution_capacity(profile_summary: str, decision_vars: dict, decision_brief: str) -> str: ...

@mcp.tool()
def run_risk_stress_test(world_state: dict, scenario_id: str, decision_brief: str) -> str: ...
```

运行方式：`python -m app.mcp_server.server`（stdio JSON-RPC）

### 7.2 工具实现

**文件**：`app/mcp_server/tools.py`

| 工具 | 实现 | 外部依赖 |
|---|---|---|
| `search_knowledge` | 调 HybridRetriever，格式化 `[source] (score=x) document` | ChromaDB + SiliconFlow |
| `search_web` | 调 TavilySearchTool，API 故障静默兜底 | Tavily API |
| `assess_execution_capacity` | 基于画像的确定性评估（不调外部服务） | 无 |
| `run_risk_stress_test` | 基于世界状态的确定性压力测试 | 无 |

**懒加载单例**：`_get_retriever()` 和 `_get_tavily()` 复用实例。

### 7.3 客户端双模式

**文件**：`app/mcp_server/client.py`

```python
class McpToolClient:
    def __init__(self, mode: str = "inline"):
        # inline (默认): 同进程直接引用 tools.py 函数
        # stdio: spawn MCP server 子进程，JSON-RPC 通信
```

| 模式 | 适用场景 | 特点 |
|---|---|---|
| `inline` | 开发/测试 | 零进程开销，直接函数调用 |
| `stdio` | 生产 | 进程隔离，标准 MCP 协议通信 |

**容错**：`search_knowledge` 失败返回 "知识库暂不可用"；其他工具失败返回空字符串。

### 7.4 角色工具路由

**文件**：`app/agents/tool_router.py` (208 行)

```python
class RoleToolRouter:
    # 角色工具白名单
    ROUTES = {
        "market":       ["search_knowledge"],
        "environment":  ["search_knowledge"],
        "personal":     ["search_knowledge", "assess_execution_capacity"],
        "risk":         ["search_knowledge", "run_risk_stress_test"],
    }
```

**Evidence 状态**：`disabled` / `empty` / `hit` / `error` / `local`

**降级链**：MCP 优先 → Retriever 直连 → 本地 fallback

---

## 8. 记忆持久化与上下文管理

### 8.1 三层记忆架构

| 层 | 实现 | 用途 | 生命周期 |
|---|---|---|---|
| 短期 | LangGraph Checkpointer (MemorySaver / SqliteSaver) | 图执行中断点恢复 | 单次推演 |
| 会话 | SQLAlchemy 9 表 + 7 DAO | 推演历史、消息、事件、画像 | 持久 |
| 长期 | AgentMemoryStore (Chroma, 按 user_id 隔离) | Agent 跨会话记忆 | 永久 |

### 8.2 增量落库

**引擎内 `engine.py`**：

```python
# 每年推演后
def _persist_agent_events(self, session_id, year, events):
    EventRepo.save_batch(session_id, events)
    # 每个 AgentAction 落一条 simulation_events

def _persist_session_state(self, session_id, state):
    SimulationRepo.update(session_id, {
        "current_year": state.current_year,
        "world_state": state.world_state,
        "timeline": state.timeline,
        "phase": state.phase,
        # ... 白名单字段
    })
```

**事务边界**：DAO 方法不管理事务，由 API 层控制 `commit` / `rollback`。`db.flush()` 保证可见性。

### 8.3 画像快照冻结

推演启动时，**冻结用户画像**到 `simulation_sessions.user_profile`（JSON 列）：

```python
session = SimulationRepo.create(
    scenario_id=scenario_id,
    user_profile=user_profile.model_dump(),  # 快照
    decision_vars=source.decision_vars,
    ...
)
```

用户后续修改画像不影响历史推演——保证可复现性。

### 8.4 上下文管理策略

| 策略 | 实现 |
|---|---|
| 世界状态定性化 | `_format_world_state()` — 数值转趋势描述，防复述 |
| RAG 优先压缩 | 不盲目扩大上下文窗口，reranker 后只取 top-k |
| 外部内容清洗 | `sanitize_rag_content()` 屏蔽 traceback/路径 |
| 不可信标签包裹 | `[UNTRUSTED_RAG]` / `[UNTRUSTED_SEARCH]` |
| Judge 反馈注入 | 修订循环时注入 `judge_feedback` + `constraint_note` |
| 工具证据传递 | `evidence` 字段记录工具调用状态和结果摘要 |

### 8.5 Checkpointer 断点续推

```python
# graph.py
def build_outer_graph(source, checkpointer=None):
    graph = StateGraph(OuterState)
    # ... 添加节点和边
    return graph.compile(checkpointer=checkpointer)

# engine.py
if config.checkpointer_url == "memory":
    checkpointer = MemorySaver()
elif config.checkpointer_url.startswith("sqlite"):
    checkpointer = SqliteSaver.from_conn_string(config.checkpointer_url)
# 生产: PostgresSaver
```

推演中断后，可通过 `thread_id` 恢复到断点继续执行。

---

## 9. 评分 / 结局 / 风险 / 干预

### 9.1 评分系统

**文件**：`app/engine/scoring.py` (422 行)

**四维评分** `compute_score()`：

支持两种模式：
- **场景化评分** `_contextual_score()` — 7 套独立四维标签
- **通用商业评分** — market / resource / profitability / risk

| 场景 | 四维标签 |
|---|---|
| 创业 | market / resource / profitability / risk |
| 考研 | knowledge / mental / time / risk |
| 留学 | language / academic / finance / adaptability |
| 职业晋升 | skill / network / opportunity / risk |
| 求职 | competitiveness / strategy / resource / risk |
| 买房 | financial / market / location / risk |
| 投资 | return / risk_control / diversification / timing |

**优先级权重**：survival / profit / growth / balanced

**结局加成**：goal_reached(+8) / steady(+4) / bankrupt(-20) / paused(-5)

### 9.2 结局判定

**文件**：`app/engine/ending.py` (96 行)

纯阈值比较，LLM 不参与：

```python
def judge_ending(world_state, year, end_conditions, success_definition):
    # 优先级: bankrupt → goal_reached → steady_state → timeout
    if cash_flow < 0:
        return "bankrupt"
    if payback_ratio >= target:
        return "goal_reached"
    if stable for N years:
        return "steady_state"
    if year >= max_years:
        return "timeout"
```

**用户自定义覆盖**：`success_definition.target_monthly_profit` 覆盖 steady 阈值。

### 9.3 风险提取

7 套场景化风险函数，每套 5 条 `RiskItem`：

```python
@dataclass
class RiskItem:
    metric: str        # 风险指标
    severity: float    # 0-1
    current_value: str # 当前值
    message: str       # 中文风险描述
```

例：创业场景风险 — 现金流风险、竞争加剧、客流下降、成本上升、回本周期延长。

### 9.4 行动计划

36 个场景化行动模板 `_ACTION_TEMPLATES`：

```python
@dataclass
class ActionPlanItem:
    metric: str       # 关联指标
    action: str       # 行动建议
    quantity: str     # 量化目标
    deadline: str     # 截止时间
```

### 9.5 干预机制

**触发条件**：场景 JSON 中的 `intervention_rules` 定义触发阈值。

```python
# intervention_graph.py
def check_and_apply(state):
    pending = find_pending_intervention(state)
    if pending and state.choice is None:
        interrupt(pending.model_dump())  # 暂停！
    if state.choice:
        apply_intervention(state, pending, state.choice)
        # 记录 InterventionRecord
```

**流程**：
1. 推演中检测到待决干预 → `interrupt()` 暂停
2. yield `INTERVENTION_PENDING` + phase=`PAUSED`
3. 用户通过 `/resume` 提交 choice
4. 恢复 invoke，应用效果，继续推演

**高危保护**：权限不足或高危操作切到 `REVIEW` / `PAUSED`。

---

## 10. LLM 分层路由

**文件**：`app/core/llm.py` (176 行)

### 10.1 三层路由

| 层 | 模型 | 用途 | 调用占比 |
|---|---|---|---|
| FAST | DeepSeek `deepseek-chat` | Agent 年度决策 | ~80% |
| SLOW | DeepSeek `deepseek-reasoner` | Judge 校验 + 行动计划 | ~15% |
| STUB | StubLLM（确定性） | 测试/开发 | — |

```python
class LLMRouter:
    def get(self, tier: ModelTier) -> BaseChatModel:
        if tier == ModelTier.FAST:
            return self._fast_llm
        elif tier == ModelTier.SLOW:
            return self._slow_llm
        else:
            return StubLLM()
```

### 10.2 StubLLM

```python
class StubLLM(BaseChatModel):
    # 不调外部 API
    # 返回固定 JSON
    # judge 模式: {"judge_ok": true, "severity": 0.0, ...}
    # agent 模式: {"action_id": "...", "reasoning": "...", "confidence": 0.8}
```

实现 `BaseChatModel` 接口，与真实 LLM 无缝替换。

### 10.3 意图分类

```python
def classify_intent(task: str) -> str:
    # agent_decision → FAST
    # judge_check → SLOW
    # action_plan → SLOW
    # scoring → STUB (纯计算)
    # other → FAST
```

### 10.4 决策推演温度

默认低温 `0~0.3`，保证推演结果可复现。

---

## 11. 安全与中间件

### 11.1 中间件栈

**文件**：`app/main.py` (186 行)

注册顺序 = 执行顺序：

```
1. RequestIdMiddleware  — 注入 X-Request-ID（链路追踪）
2. ApiKeyMiddleware     — API Key 认证
3. RateLimitMiddleware  — 速率限制
4. BodyLimitMiddleware  — 请求体大小限制
5. CORS                 — 跨域
6. Gzip                 — 压缩
7. SecurityHeadersMiddleware — 安全头 (X-Content-Type-Options 等)
```

### 11.2 Prompt 注入防护

**文件**：`app/core/sanitize.py` (101 行)

`sanitize_user_input(text, max_length=2000)`：
- 截断超长输入
- 去除控制字符（除 \n \t）
- 7 种注入模式检测（中英文）：
  - "ignore previous instructions"
  - "you are now DAN"
  - `<system>` 标签
  - "forget everything"
  - 中文"忽略之前指令"
  - 角色扮演注入
  - 提示词泄露

`sanitize_rag_content(text)`：
- 屏蔽 Python traceback
- 屏蔽文件路径 (`File "xxx.py", line N`)
- 去除控制字符

### 11.3 统一错误兜底

```python
# 全局异常处理器
@app.exception_handler(Exception)
async def global_exception_handler(request, exc):
    return JSONResponse(
        status_code=500,
        content={
            "code": "INTERNAL_ERROR",
            "message": "服务内部错误，请稍后重试",
            "request_id": request_id,
        }
        # 不暴露原生堆栈
    )
```

### 11.4 JWT 认证

**文件**：`app/core/jwt.py`

- Token 签发 / 验证
- `JWT_SECRET` >= 32 bytes（启动校验）
- `access_token_expire_minutes` 可配

---

## 12. 数据库设计

### 12.1 九表总览

**文件**：`app/db/models.py` (312 行)

| 表名 | 用途 | 关键设计 |
|---|---|---|
| `users` | 用户 | email unique, bcrypt 密码哈希 |
| `scenarios` | 场景元数据 | scenario_id unique, decision_source JSON |
| `assets` | 场景资源 | UQ(scenario_id, kind, ref_id) |
| `kb_chunks` | 知识库分块 | embedding JSON, industry/city/type/tags |
| `simulation_sessions` | 推演会话 | 40+ 字段, user_profile 快照, timeline/score/risks/action_plan JSON |
| `simulation_messages` | 推演消息 | session_id FK, sequence 自动递增 |
| `simulation_events` | 推演事件 | session_id FK, state_diff/payload JSON |
| `user_profiles` | 用户画像 | 6 维度 JSON |
| `agent_memories` | Agent 记忆 | UQ(user_id, agent_id, domain, key) |

### 12.2 设计要点

- **主键**：`String(36)` + `uuid4()`
- **JSON 列**：`sqlalchemy.JSON`（开发 SQLite）/ `JSONB`（生产 PostgreSQL）
- **时间戳**：`DateTime(timezone=True)`
- **画像快照**：`simulation_sessions.user_profile` — 推演启动时冻结
- **决策日记**：`diary_tags` / `diary_notes` / `archived` — 用户事后标注
- **现实校准**：`actual_result` / `calibration_score` — 推演结果与现实对比

### 12.3 DAO 仓库

**文件**：`app/db/repository.py` (580 行)

7 个仓库类，全部不管理事务：

| 仓库 | 核心方法 |
|---|---|
| SimulationRepo | create, update (白名单), get, list_by_scenario/user/owner |
| MessageRepo | save (自动 sequence), save_batch, list_by_session |
| EventRepo | save, save_batch, get_by_session |
| ScenarioRepo | upsert, get, list_all |
| AgentMemoryRepo | save, get, delete_by_domain |
| KbChunkRepo | save_batch (幂等), query (industry/city/type 筛选) |
| ProfileRepo | get, create, update, list_all |
| DiaryRepo | update_diary, list_diary, save_calibration (校准计算) |

### 12.4 幂等迁移

**文件**：`app/db/session.py`

```python
_ADD_COLUMNS = {
    "simulation_sessions": [
        ("diary_tags", "JSON"),
        ("actual_result", "JSON"),
        ("calibration_score", "FLOAT"),
        # ...
    ],
}
_ADD_INDEXES = [
    ("simulation_sessions", "idx_sessions_user", ["user_id"]),
    # ...
]

def init_db():
    Base.metadata.create_all(engine)
    _migrate()  # 幂等 ALTER TABLE 补列 + 补索引
```

开发期替代 Alembic，新增字段时自动补列。

---

## 13. 答辩 Q&A 预演

### Q1: 为什么用两层 LangGraph 而不是一层？

**A**: 外层管时间线推进（年度循环），内层管 Agent 协调（一轮辩论）。这两者的关注点不同：时间线推进是线性的（prepare→apply→check→append），而 Agent 协调需要条件边循环（judge 不通过要回去 revise）。混在一个图里会导致状态字段混乱、条件边复杂度爆炸。分离后各自独立编译，可单独测试，可独立替换 checkpointer。

### Q2: Judge 修订循环最多 2 次，会不会不够？

**A**: 2 次是经验值。第 1 次修订通常能解决 80% 的冲突（注入反馈 + 收窄白名单），第 2 次处理剩下的顽固冲突。超过 2 次说明 Agent 之间立场根本对立——这时强制通过，让评分系统和结局判定来体现后果，比无限循环更合理。这也是防止 LLM 幻觉导致死循环的兜底。

### Q3: RAG 为什么用混合检索而不是纯向量？

**A**: 纯向量检索在中文场景下有短板——bge-m3 对专业术语的语义理解有限，关键词匹配反而更精准。BM25 补了精确匹配能力。但两者各有盲区，RRF 融合后用 Reranker 重排取精华。这套管道在创业/留学/投资等场景的检索准确率明显优于单一方式。另外三级容错（Embedding 挂走 BM25，Reranker 挂走 RRF）保证了可用性。

### Q4: 决策源驱动的红线具体怎么执行？

**A**: 三个层面。第一，`scenarios/*.json` 定义事实、约束、动作效果和结束条件——这些是数据不是代码，LLM 看不到也改不了。第二，`nodes.py` 是纯函数不调 LLM，`scoring.py` 和 `ending.py` 也是纯计算。第三，结局判定 `judge_ending()` 是纯阈值比较，LLM 的输出只影响 world_state 的变化方向，不直接决定结局。

### Q5: MCP 工具为什么用双模式？

**A**: inline 模式在开发/测试时用——同进程直接调函数，零进程开销，调试方便。stdio 模式在生产用——子进程隔离，标准 MCP 协议通信，工具崩溃不影响主进程。McpToolClient 封装了两种模式的切换逻辑，上层代码无感知。

### Q6: 提示词工程的核心设计是什么？

**A**: 四个关键点。第一，System/User Prompt 严格分离——System 定义角色和格式约束，User 提供具体决策上下文。第二，世界状态定性化——不让 LLM 看精确数值（如 cash_flow=35000），而是看趋势描述（"资金缓冲较为紧张"），防止复述数值而非推理。第三，外部内容用 `[UNTRUSTED]` 标签包裹 + 清洗防注入。第四，JSON 解析三重容错——直接解析 → markdown 代码块 → 栈平衡括号。

### Q7: 记忆持久化怎么设计的？

**A**: 三层。短期记忆用 LangGraph Checkpointer，支持推演中断后断点续推。会话记忆用 SQLAlchemy 9 表，每年推演增量落库（事件 + 状态更新），事务边界由 API 层控制。长期记忆用 AgentMemoryStore（Chroma，按 user_id 隔离），Agent 可以跨会话积累经验。另外画像快照在推演启动时冻结到 session 表，保证历史推演可复现。

### Q8: 前端 SSE 流式推演怎么实现的？

**A**: 后端用 `sse-starlette` 的 `EventSourceResponse`，引擎的 `aiter_events()` 是异步生成器，逐年 yield SSE 事件（agent_proposals / judge_result / world_state / year_summary）。前端通过 EventSource API 接收，Pinia store 管理推演进度，实时更新 Agent 面板、辩论记录、世界状态图表和时间线。

### Q9: 安全方面做了哪些防护？

**A**: 五层。第一，7 层中间件栈（RequestId→ApiKey→RateLimit→BodyLimit→CORS→Gzip→SecurityHeaders）。第二，Prompt 注入防护——7 种注入模式检测 + 控制字符过滤 + 超长截断。第三，RAG 内容清洗——屏蔽 traceback 和文件路径。第四，JWT 认证——Token 签发验证 + Secret >= 32 bytes。第五，统一错误兜底——不暴露原生堆栈，统一返回 `{code, message, request_id}`。

### Q10: 项目的最大技术挑战是什么？

**A**: 多 Agent 协调的一致性。四个 Agent 各有立场（市场激进、风险保守、个人稳健、环境关注），它们的提案可能直接冲突（如市场要扩张 vs 风险要收缩）。解决方案是 Judge 修订循环——检测冲突后注入反馈、收窄白名单、最多 2 次重试。这个机制让 Agent 辩论从"各自为战"变成"有约束的协商"，最终输出是各方妥协后的可行方案。

---

## 附录：关键文件索引

| 模块 | 文件 | 行数 |
|---|---|---|
| 外层 Graph | `app/engine/graph.py` | 79 |
| 纯函数节点 | `app/engine/nodes.py` | 49 |
| 评分系统 | `app/engine/scoring.py` | 422 |
| 结局判定 | `app/engine/ending.py` | 96 |
| 干预 Graph | `app/engine/intervention_graph.py` | 82 |
| 主引擎 | `app/engine/engine.py` | 1401 |
| 量化创业 | `app/engine/yanjie_engine.py` | 189 |
| 内层 Graph | `app/agents/inner_graph.py` | 739 |
| LLM Agent | `app/agents/llm_agent.py` | 587 |
| Agent 契约 | `app/agents/contracts.py` | 39 |
| 工具路由 | `app/agents/tool_router.py` | 208 |
| Judge | `app/agents/judge.py` | 281 |
| 混合检索 | `app/kb/retriever.py` | 209 |
| 摄入管道 | `app/kb/ingest.py` | 95 |
| 文本切分 | `app/kb/splitter.py` | 97 |
| 场景路由 | `app/kb/classify_scene.py` | 180 |
| ChromaStore | `app/kb/chroma_store.py` | 153 |
| VectorStore | `app/kb/vector_store.py` | 57 |
| Agent 记忆 | `app/kb/agent_memory_store.py` | 109 |
| MCP Server | `app/mcp_server/server.py` | 78 |
| MCP Client | `app/mcp_server/client.py` | 88 |
| MCP Tools | `app/mcp_server/tools.py` | 132 |
| 数据库模型 | `app/db/models.py` | 312 |
| DAO 仓库 | `app/db/repository.py` | 580 |
| 数据库会话 | `app/db/session.py` | 133 |
| API 路由 | `app/api/simulation.py` | 537 |
| SSE 流 | `app/api/stream.py` | 109 |
| FastAPI 入口 | `app/main.py` | 186 |
| LLM 路由 | `app/core/llm.py` | 176 |
| 配置中心 | `app/core/config.py` | 149 |
| 安全防护 | `app/core/sanitize.py` | 101 |

---

> **slogan**: 让每一个选择都有回响。
