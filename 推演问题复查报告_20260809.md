# 推演问题复查报告 2026-08-09

> 复查对象：2026-08-06《推演功能代码审查报告》列出的 B1–B9 与 2026-08-09《全局代码审查报告》的 G1–G7。
> 方法：代码比对（项目无 git，比对函数签名/逻辑原样性） + 实跑脚本（真实 `run_batch` 路径 + 静态决策映射）。
> 环境：`F:/2308/python.exe`（3.12.3），`use_stub=True`，加载 `scenarios/general_startup.json`。

## 一、状态速览

| 上次项 | 上次结论 | 本次复查结果 | 状态 |
|--------|----------|--------------|------|
| B1 同步跑不出结果 | `.env` 的 `PAUSE_EACH_YEAR=1` 卡死 | 真因是 `iter_events` 在 784 行 `return`，786 的 `for` 主循环是**死代码**；`PAUSE_EACH_YEAR` 在不可达分支，**根本不执行** | **根因修正**，同步端点仍残废 |
| B2 4 个 Agent 决策被整体丢弃 | 代码未变 | 代码文字未变 | **未修** |
| B3 `aggressive` 与 `steady` 结果相同 | 相同 | 实跑现金流**逐字节相同** | **未修（确凿）** |
| B4 用户"保守"意图被无视 | 关键词表缺"保守" | 静态映射仍无"保守/稳妥/谨慎" | **未修** |
| B5 账本现金流冻结在 19098 | 冻结 | 真实路径现金正常累加 15万→31万→68万 | **误报，已纠正** |
| G1 `mcp client` `return ""` + 错误透前端 | 未修 | `client.py:81` 原样未动 | **未修** |

## 二、逐项详述

### B1（根因修正，比上次定位更深）
上次把"同步推演跑不出结果"归因为 `.env` 的 `PAUSE_EACH_YEAR=1`。本次实读 + 实跑后修正：

- `engine.py:880` 的 `os.getenv("PAUSE_EACH_YEAR") == "1"` 判断位于 `iter_events` 的 `for` 主循环（786 行）**内部**，而 `iter_events` 在 **784 行已 `return`**（构造初始 `paused_state` 后直接返回），主循环**不可达**。
- 因此 `PAUSE_EACH_YEAR` 是**死配置**，既不执行、也不是 B1 根因，更不该作为修复点。
- 真正根因：`iter_events` 无条件在 784 行 `return`，同步 `run()`（→ `POST /api/simulations` 同步端点 `simulation_service.run` → `engine.run`）**永远只返回 paused 初态**：`timeline=0, score=None`。
- **但**流式端点（`aiter_events` + 前端逐年 `resume`）与 `run_batch`（A/B 对比）走 langgraph `_iv_graph` + checkpoint 的 `resume_events` 路径，**能跑完整推演**。本次实跑 `run_batch` 得到 3 年完整 `timeline` + `score=90.54`。

**影响**：凡依赖同步端点的调用/测试都拿不到结果；`iter_events` 786 行起的 `for` 主循环是误导性的死代码。

### B2（未修，确认）
`_advance_startup_ledger`（`engine.py:154-222`）文字未变：4 个 Agent 的 `actions` 仅用于补 `reason/confidence`，`decision_id` 仍来自 `_startup_decision_id(user_msg, strategy)`，**不读 Agent 共识**。general_startup 下 4 个智能体对量化决策不起作用。

### B3 / B4（未修，实锤）
- 静态映射：`aggressive` 与 `steady` 三种输入（扩张词 / 保守词 / 空）产出**完全一致**（均走 `steady_growth` / `precision_breakthrough`）。
- 实跑（`run_batch`，`strategy_directives` 分别全 `aggressive` / 全 `steady`）：现金流序列**逐字节相同**
  `[150344.86, 316556.01, 682032.08]`。
- "我想保守稳妥谨慎经营"仍映射到 `steady_growth`——关键词表仍无"保守/稳妥/谨慎"，用户保守意图被无视。

### B5（误报，已纠正）
上次 8/6 称"现金流全年冻结在 19098"。本次在真实 `run_batch` 路径实跑：现金正常累加 `150344 → 316556 → 682032`，季度利润递增，无冻结。
**判定**：上次复现方法有误（同步 `run` 跑不出推演，当时构造的对比态不可靠），B5 在真实路径不复现，**从 bug 清单移除**。
备注：3 年现金翻 4.5 倍、`score=90.54` 增长偏乐观，建议校准创业成本/订单参数（原 B6），但非"冻结"硬 bug。

### G1（未修，确认）
`mcp_server/client.py:81` 工具调用失败 `return ""` 原样未动；叠加 `ask_service` / `ask.py` 把 `str(exc)[:200]` 直返前端，违反 AGENTS.md"不暴露原生堆栈"红线。

## 三、本次新发现

- **N1（死代码）**：`iter_events` 786 行起的 `for` 主推演循环被 784 行 `return` 截断，**完全不可达**，应删除或重构，否则误导维护者。
- **N2（端点残废）**：同步 `run()`（`POST /api/simulations`）注释称"无交互入口"，但实际需前端 `resume` 才能推进，单独调用永远返回 paused。要么修到能跑完整，要么文档明确"同步端点不可用，请用 stream+resume"。
- **N3（死配置）**：`PAUSE_EACH_YEAR` 配置项**完全无效**（代码在不可达分支），属死配置，应移除或重新接入。

## 四、修复优先级（修订）

- **P0（真阻断）**：B2（让 Agent 共识驱动决策）/ B3+B4（策略与用户意图真正生效）/ N2（修同步端点或明确弃用）。
- **P1**：G1（错误透传 + 兜底话术）/ N1+N3（清死代码与死配置）/ B6（校准创业成本参数，抑制偏乐观增长）。
- **已移除**：B5（误报）。

## 五、诚实声明

上次 2026-08-06 报告的 **B1（PAUSE 归因）与 B5（现金流冻结）结论需修正**：B1 根因更深（`iter_events` 死 `return`，非 PAUSE）；B5 在原复现方法下是误报。其余 B2/B3/B4/G1 经本次实跑与代码比对确认**未修复**。项目自 8/6 以来无 git 提交、核心推演代码文字层面无改动。
