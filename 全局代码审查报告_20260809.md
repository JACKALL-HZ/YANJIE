# 全局代码审查报告 · 2026-08-09

> 审查范围：整个后端 `app/`（101 个 py 文件）+ 测试套件（59 文件 / 363 函数）
> 方法：静态扫描（异常/凭证/死代码/危险函数）+ 关键模块精读（engine / services / mcp / db / api / core）+ 测试规模统计
> 注：全量 `pytest` 在后台运行（>1.5min 未出，疑似 Chroma/LLM stub 初始化开销大），通过/失败数待补。

## 一、整体印象

**好的一面（该夸的夸）：**
- 安全地基干净：无硬编码 API Key/密码、无 `eval/exec`、无 `print` 调试残留、无裸 `except:`。
- 数据访问层设计规范：`SimulationRepo` 有字段更新白名单 `_UPDATE_ALLOWED`、场景 `upsert` 幂等、`MessageRepo.decode_role` 做了历史兼容、`AskService` 用了 `sanitize_user_input` / `sanitize_rag_content` 做输入清洗与 RAG 脱敏。
- 接口有权限校验：`api/ask.py` 用了 `assert_session_owner` + `get_request_actor`（owner 鉴权）。
- 测试规模够：363 个测试函数覆盖 101 个源文件（≈3.6:1）。

**最大问题（一句话）：**
异常被**批量静默吞掉**，且部分内部报错**漏回前端**——可观测性几乎为零，问题发生时系统"看起来还活着，其实已经歪了"。这与 8/6 审查发现的 B5「现金流冻结静默发生」是同一种基因。

## 二、五轴审查

### 🔴 正确性 / 安全（ blockers 级）

**G1 异常静默吞 + 内部错误泄露前端**
- 全库 `except Exception` / `except Exception as exc` 出现 **30+ 处**（engine / agents / kb / mcp / services / api / core）。
- 其中 `app/mcp_server/client.py:78` `McpToolClient.call()` 失败时 `return ""`（知识库）或 `return ""`——**调用方无法区分"无结果"还是"工具挂了"**，推演会静默缺数据还以为正常。
- `app/core/llm.py:174` `classify_intent` 异常直接 `return "agent_decision"`（fail-open，掩盖意图分类器故障）。
- **更严重**：`app/services/ask_service.py:86` 与 `app/api/ask.py:61` 把 `str(exc)[:200]` 直接返回给用户 / SSE error 帧——违反 `AGENTS.md` 红线「异常不应暴露原生堆栈」。

**G2 上次推演 B1–B9 全部未修复**
项目无 git 提交记录（`AGENTS.md` 确认）、代码未变更，8/6 报告中的 B1（同步推演卡 Y1）、B2（4 Agent 决策被丢弃）、B3/B4（策略与意图错乱）、B5（现金流冻结）等仍在。

### 🟡 可维护性 / 性能（suggestion 级）

**G3 DAO 批量写入 N+1**
`app/db/repository.py:181` `MessageRepo.save` 每次都 `func.max(sequence)` 查一次最大序号；`save_batch` 循环调用 → 存 N 条消息 = N 次 `SELECT MAX`。应一次查出当前 max 后内存自增。

**G4 决策校准算法过弱**
`app/db/repository.py:556` `save_calibration` 用「结局字符串字面相等」判分（相等=1.0，不同但有值=0.5，否则=0）。无语义相似度，校准分几乎无业务意义。

**G5 异常处理风格不一致**
有的吞异常后 `_log.exception`（ask_service / stream.py / api/ask.py），有的**完全没 log 直接吞**（mcp_client、classify_intent、inner_graph:492/504、judge.py:251、ingest.py:87、retriever.py:62、tool_router.py 多处）。同一项目的错误处理策略不统一，排障靠运气。

**G6 内存过滤 / 静默忽略字段**
- `app/db/repository.py:539` `list_diary` 先 `q.all()` 再 Python 层按 `tag` 过滤——应在 SQL 层用 JSON 包含查询。
- `app/db/repository.py:85` `update()` 静默 `continue` 跳过白名单外字段——是安全设计，但调用方拼错字段名会被吃掉，可能掩盖 bug（双刃剑）。

### 💭 测试（nit 级）

**G7 覆盖盲区 + 慢测试无标记**
- `general_startup` **完整推演路径无端到端测试**（这正是 B1–B9 的发生地）；`test_yanjie_engine.py` 只测孤立账本，CI 抓不到编排层 bug。
- 全量 `pytest` 跑 >1.5min 仍无输出，疑似集成测试初始化 Chroma/LLM 无 `pytest.mark` 拆分，拖慢整个套件。

## 三、优先级清单

| 级别 | 项 | 动作 |
|------|----|------|
| 🔴 P0 | G1（MCP 返回空串 + 报错泄露前端） | MCP `call()` 失败抛/记结构化错误而非空串；ask 类接口返回**统一兜底话术**，错误细节只进日志 |
| 🔴 P0 | G2（B1–B9 推演 bug） | 见 8/6 报告，先修 B1/B2/B5 |
| 🟡 P1 | G3（MessageRepo N+1） | `save_batch` 一次取 max 后自增 |
| 🟡 P1 | G5（异常风格统一） | 定义统一错误包装：吞异常必须 `_log.exception` + 返回结构化错误，禁止静默 `return ""` |
| 🟡 P1 | G7（测试盲区） | 补 `general_startup` 端到端推演测试 + 给慢测试加 marker |
| 💭 P2 | G4（校准算法） | 换成语义相似度或结构化结局比对 |
| 💭 P2 | G6（内存过滤/字段忽略） | tag 过滤下沉 SQL；`update()` 非法字段改为显式告警 |

## 四、行动建议

1. **先立一条铁律**：任何 `except` 块要么重抛、要么 `_log.exception` + 返回结构化错误，**禁止 `return ""` / 静默 pass**。这条能直接灭掉 G1/G2/G5 的大部分隐患。
2. **P0 两件事并行**：修 MCP 错误透传 + 修 B1–B9 推演主链路，否则"推演跑不对、错了还看不出"会一直背在身上。
3. **测试补盲区**：`general_startup` 端到端测试一补，下次类似 B5 的回归 CI 立刻能抓。

> 上次专项（推演）与本次全局审查可合并为一站式整改清单：推演正确性（B1–B9）+ 异常/可观测性（G1/G5）+ DAO 性能（G3）+ 测试（G7）。
