# 内层图（辩论图）各节点作用详解

> 配套之前的问答重生成：知识库空怎么办 → 在哪拼提示词 → 哪个节点拼。
> 内层图是**一年推演内部**的"四角色辩论"，由 `SimulationEngine` 逐年调起。
> 注册位置：`app/agents/inner_graph.py:132-138`。

## 一句话流程

```
observe → propose → judge →（revise ↺ 最多2次）→ validate → emit
                 ↑________条件边回退________│
```

大白话：备料 → 四个 Agent 各自炒菜出决策 → 裁判评审 → 不行就退回重做 → 最后质检装盘 → 交差。

---

## 1. observe（观察 / 备料）

**大白话**：洗菜切菜。给四个角色各造一份"原料包"，但**不炒菜（不拼提示词）**。

**代码位置**：`_observe` (line 70) → `coordinator.observe()` (line 330-384)

**干什么**：
- 调 `_tool_router.build_all(state, decision_brief, profile_summary)` (line 341) → 给每个角色造**工具证据**（RAG/MCP 检索结果）。这就是之前讲的 RoleToolRouter：按角色白名单分配工具、用"场景ID+年份+角色焦点+决策变量+用户决策"四要素拼 query。
- 从 `state` 采原料：`world_state`、`decision_vars`、`allowed_action_ids`、约束 `agent_constraints`、`scenario_brief`。
- 算 `rag_status`（hit / empty / error / disabled，line 380）和 `rag_sources`（line 381）——**知识库没内容就在这一步标成 `empty`**，后面会锁死模型不编。
- 每个角色拿**不同的 evidence** 塞进各自的 `AgentContext`（line 357 / 382）。

**关键**：产出的是 `AgentContext`（原料包），不是提示词。

---

## 2. propose（提案 / 下锅）

**大白话**：四个 Agent 各自拿原料炒菜——**拼提示词、调 LLM、各出一条决策**。

**代码位置**：`_propose` (line 80) → `coordinator.propose_actions()` (line 548)

**干什么**：
- 并行跑 `agent.propose(context)` (line 557，ThreadPoolExecutor 并行 line 569)。
- 每个 `LlmAgent.propose()` (llm_agent.py:189) → 内调 `_build_messages()` (llm_agent.py:200) **真正拼提示词**：
  - `SystemMessage(_SYSTEM_PROMPT.format(...))` (line 283)
  - `HumanMessage(_USER_PROMPT.format(...))` (line 353)
  - 外部内容（RAG/搜索/用户输入）全过 `_untrusted_block()` 清洗脱敏。
  - **`rag_status` 不是 hit 时，System Prompt 硬约束模型"不得声称有外部资料支持"**（llm_agent.py:92）——这就是知识库空的兜底。
- 把 `rag_status / rag_sources / evidence` 回写进 `AgentAction`（line 558-563）。

**关键**：**拼提示词 + 调模型发生在这个节点**（四角色都在这里拼）。

---

## 3. judge（裁判评审 / 尝菜）

**大白话**：裁判尝菜，看四份决策有没有打架、有没有胡说、格式对不对。

**代码位置**：`_judge` (line 83) → `coordinator.judge_round()` (line 573) → `Judge.judge()`

**干什么**：
- 在 `app/agents/judge.py:240-241` **单独拼一套提示词**（`_JUDGE_SYSTEM` / `_JUDGE_USER.format(...)`），逻辑与 propose 同源（也带 `rag_status`、evidence）。
- 返回 `JudgeResult`：`judge_ok`（过没过）、`severity`（严重度）、`conflicts`（冲突清单）、`recommendations`（协调要求）。

**关键**：裁判也有自己的提示词拼装，但只在 `judge` 节点。

---

## 4. _route_after_judge（条件边 / 不是节点，但是关键）

**大白话**：裁判说"不行"就退回重做；说"行了"就装盘；重做超 2 次就算了强行装盘。

**代码位置**：`_route_after_judge` (line 118-124)

```
judge_ok == True          → validate      （直接放行）
judge_ok == False 且 revision_count < 2 → revise   （回退重做）
judge_ok == False 且 revision_count >= 2 → validate （强制放行）
```

> 之前你问过"这图没有条件边"——条件边就在这里，它决定 `judge` 之后是去 `validate` 还是回 `revise`。

---

## 5. revise（修订重做 / 回锅）

**大白话**：把裁判的吐槽翻译成中文反馈，塞回相关 Agent 的上下文，限死它能选的动作，再跑一轮 `propose`。

**代码位置**：`_revise` (line 88-116)

**干什么**：
- 把 `JudgeResult` 的 `conflicts` + `recommendations` 拼成中文 `feedback_text`（line 92-100）。
- `register_conflict_constraints()` 生成约束（收窄动作范围）。
- `apply_constraints_to_contexts()` 把约束注入各角色 context（line 104）。
- 给每个 context 填 `judge_feedback=feedback_text`（line 111）——下次 `propose` 拼提示词时这段会进 User Prompt。
- `revision_count + 1`（line 115），然后图回到 `propose` 重跑。

**关键**：revise 本身**不调 LLM**，它只是改造 context，真正的重做发生在下一轮 `propose`。

---

## 6. validate（校验守门 / 质检）

**大白话**：装盘前最后质检——理由不能空、动作必须合法、乱码换成兜底话术、补齐展示字段。

**代码位置**：`_validate` (line 126) → `coordinator.validate()` (line 641-706)

**干什么**：
- `reason` 为空 → 抛错（line 652）。
- `action_id` 不在 `allowed_action_ids` → 替换成第一个允许的 + 记 warning（line 654-660）。
- `normalize_agent_reason()` 规整文案（line 661）。
- 乱码/不可读 → `build_stub_reason()` **兜底话术**替换（line 664-675）——呼应"统一兜底不暴露堆栈"。
- 补齐 `recommendation / alternatives / objection / stop_condition` 展示字段（line 676-704）。

---

## 7. emit（输出 / 交差）

**大白话**：把四份最终决策**深拷贝**出来交给外层图，防止后面改动污染内部状态。

**代码位置**：`_emit` (line 129) → `coordinator.emit()` (line 708-709)

**干什么**：`[action.model_copy(deep=True) for action in actions]`（line 709），纯透传 + 拷贝。

---

## 速查表

| 节点 | 大白话 | 代码位置 | 核心动作 | 拼提示词？ |
|---|---|---|---|---|
| observe | 备料 | inner_graph.py:330 | 造 AgentContext + 调 RAG/MCP + 算 rag_status | 否（只造原料） |
| propose | 炒菜出决策 | inner_graph.py:548 → llm_agent.py:200 | 并行调 LLM、出 AgentAction | **是**（四角色各拼） |
| judge | 裁判尝菜 | inner_graph.py:573 → judge.py:240 | 评审冲突/风险/格式 | **是**（裁判专用） |
| revise | 回锅重做 | inner_graph.py:88 | 注裁判反馈+约束回 context | 否（改 context） |
| validate | 质检 | inner_graph.py:641 | 合法性/乱码兜底/补齐字段 | 否 |
| emit | 交差 | inner_graph.py:708 | 深拷贝输出 | 否 |

**一句话锚点**：`observe` 备料不拼词，`propose` 和 `judge` 才拼提示词；知识库空在 `observe` 标 `empty`，在 `propose` 用提示词锁死模型不编。
